import { PageGroup } from '../../types/PageGroup'

import article0 from './golden-white-noise/ref'

const group: PageGroup = {
    name: 'glsl',
    pages: [
        article0,
    ]
}

export default group